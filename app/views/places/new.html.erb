<body class="newcity"

  <div class="container form">
    <div class='card formbox' style="width: 30rem;">
      <div class='card-block newblock'>
        <h2 class="message">Add City</h2>

        <%= form_for @place do |form| %>
          <div class='new'><%= form.label :name %></div>
          <div class='new'><%= form.text_field :name %></div>
          <div class='new add-city-button'><%= form.submit 'Add' %>
        <% end %>

        <div class='new back-to-city'><button class="btn-secondary"><%= link_to 'Back', user_path(current_user) %></button></div>
      </div>
  </div>
</body>
