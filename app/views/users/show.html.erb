<% if flash[:notice] %>
    <div class="notice"><%= flash[:notice] %></div>
<% end %>

<h1> Hi, <%= @user.email %> ! </h1>

<% if @user.places == [] %>
  <p> You have not added any cities yet. </p>
<% else %>
  <% @data.keys.each do |place| %>
    <%= place.name.capitalize %>: <%= (((@data[place]['main']['temp']) * (9.0/5)) - 459.67).round %> Farenheit
    <br>
    <%= link_to 'Delete', place_path(place), method: :delete %> |
    <%= link_to 'Details', 
    <br>
    <br>
  <% end %>
<% end %>


<%= link_to 'Add City', new_place_path(current_user) %>
